{
  "rtsp_main": "rtsp://127.0.0.1:8554/camera",
  "rtsp_camera_direct": null,
  "frame_url": "http://127.0.0.1:1984/api/frame.jpeg?src=camera",
  "model": "models/MDV6-yolov9-c.pt",
  "device": "mps",
  "fps": 3,
  "display_fps": 12,
  "imgsz": 1280,
  "conf": { "animal": 0.20, "person": 0.30 },
  "ignore_classes": ["vehicle"],
  "clahe": true,
  "roi": null,
  "exclude_roi": null,
  "min_hits": 2,
  "window": 5,
  "cooldown_secs": 30,
  "signal_timeout_secs": 8,
  "faces": {
    "enabled": true,
    "min_face_px": 45,
    "det_score": 0.7,
    "threshold": 0.40,
    "margin": 0.10,
    "vote_window_secs": 45,
    "min_votes": 2,
    "known_suppresses_event": false
  },
  "scenery": {
    "enabled": true,
    "min_move": 0.02,
    "jitter_slack": 2.5,
    "jitter_learn_secs": 5.0,
    "track_iou": 0.30,
    "iou_match": 0.60,
    "static_after_secs": 180,
    "min_sightings": 5,
    "forget_secs": 1800,
    "conf_certain": 0.70,
    "conf_override": 0.25
  },
  "talk": {
    "enabled": true,
    "greet_text": "Hallo.",
    "greet_voice": "Anna",
    "greet_on": "person",
    "greet_once": false,
    "greet_cooldown_secs": 120
  },
  "gallery": {
    "enabled": true,
    "min_gap_secs": 15,
    "dedup_cos": 0.94,
    "max_faces": 1500,
    "max_animals": 1500
  },
  "species": {
    "enabled": true,
    "verify_person": true,
    "promote_unproven": true,
    "promote_gap_secs": 5.0,
    "model": "models/speciesnet_crop_4.0.1a.pt",
    "labels": "models/speciesnet_labels.txt",
    "human_veto": 0.25,
    "human_min": 0.45,
    "species_min": 0.50
  },
  "auto_record": true,
  "buffer_secs": 120,
  "preroll": 20,
  "postroll": 15,
  "seg_secs": 2,
  "monitor_port": 8090,
  "open_browser": true,
  "email": {
    "enabled": true,
    "to": null,
    "trigger_on": ["RACCOON", "CAT", "ANIMAL", "PERSON"],
    "min_gap_secs": 120
  },
  "_comment": "roi/exclude_roi are [[x,y],...] polygons in 1920x1080 space. Detection ignores 'vehicle' (the BBQ trips it). null rtsp_camera_direct / email.to are filled in from local.env (see h32env.py). Email: set enabled=true and fill detector/secrets.json (copy secrets.json.example).",
  "_comment_scenery": "Static garden furniture reads as a low-confidence person to MegaDetector — that is what produced 56 bogus PERSON events on 2026-08-16, and then the rock that kept firing that afternoon. See scenery.py. min_move is how far a box must travel (as a fraction of its own size) before it counts as alive, and applies to a spot we have only just noticed: raise it if furniture still gets through, lower it if slow animals are missed. A spot we have watched for jitter_learn_secs is instead judged against the wobble it actually shows, times jitter_slack — the rock's box breathes by 6% of its own size while the rock does not move at all, which is MORE than the raccoon really moved, so a fixed min_move cannot separate them. Lower jitter_slack if wobbly furniture still gets through; raise it if something that genuinely moves a little is missed. iou_match is how nearly two boxes must coincide to count as the same spot: it has to be loose enough to tolerate that wobble, or one boulder sprawls into dozens of anchors and each one starts out trusting again. static_after_secs is how long a never-moving spot must keep flickering before it is written off as scenery — and only real DISPLACEMENT restarts that clock, never confidence: MegaDetector held 'person' 0.69-0.80 on a stone trough that had not shifted a pixel in 100 minutes (2026-08-17), and while confidence counted as movement the spot was detected 2470 times and never once became learnable. A learned spot is overridden by a detection more confident than that spot has ever looked itself (conf_override above its own learned max), which is the bench being beaten by a real person at 0.84; conf_certain no longer buys a blanket override, because furniture that reaches 0.80 on its own would then hold one for ever — a confident detection at a learned spot is left unproven for SpeciesNet to arbitrate instead, so a person standing at the trough is still announced and the trough is not. Learned spots live in detector/scenery.json — delete that file to forget them.",
  "_comment_switches": "auto_record and email.enabled are the STARTING positions of the two switches on the monitor (⏺ record, ✉️ e-mail; keys r and e). Both decide only what an event PRODUCES — detection never stops, every event is still listed and still written to events.log, so with both off you keep a full record of what was seen and when, without clips or mail. The live state lives in the detector (server.py /set), not in the page, so two browsers cannot disagree and a reload cannot silently re-arm e-mail; flips are logged. Deliberately NOT persisted: a restart comes back up in the configured state rather than in whatever mood the last session left it in.",
  "_comment_signal": "signal_timeout_secs: no frame for this long and the monitor says NO CAMERA SIGNAL, detection pauses and no events fire (rather than sitting on the last frame with a ticking clock).",
  "_comment_species": "SpeciesNet (see speciesnet.py) is the second opinion on the CROP: it names the animal AND checks that a MegaDetector 'person' really is one. Get it with detector/get-speciesnet.sh (~225MB, gitignored). verify_person=true lets it OVERRULE a person box: below human_veto P(human) the box is re-tagged animal, which stops the e-mail and the greeting — that is the 2026-08-16 21:18 cat that MegaDetector called 'person 0.74' and the camera said 'Hallo.' to. Measured on the archive: that cat scored P(human) 0.0017-0.0855 across 29 crops while 9 real people scored 0.49-0.999, so the 0.25 veto sits in a 5.7x gap. The rule is ONE-SIDED — it can demote a person but never invent one, and an unsure verdict leaves the event alone, so a bad call costs a false alarm rather than a missed person. species_min 0.50 is how confident it must be to name a species: the good 03:53 raccoon crop clears it at 0.978, the poor ones from 20260816_035358 sit at 0.28-0.34 and stay generic ANIMAL. This REPLACED the CLIP nearest-reference matcher in species.py, which was measured to key on lighting rather than species (empty night pavement scored raccoon 0.919 — higher than the actual cat). promote_unproven is the other direction: the scenery movement gate asks 'has it moved?' as a proxy for 'is it alive?', and a person who walks out and STANDS STILL never moves — if MegaDetector also only catches them in occasional frames the track expires between sightings, displacement is measured from the box to itself and is 0.000 for ever, so no threshold can rescue them. That is the 22:48 friend who stared at the camera and got no alert (see test_scenery.py section 9). When we would have fired but nothing cleared the gate, SpeciesNet is asked directly and a positive identification (a human, or a confidently named SPECIES) fires anyway. What keeps furniture out is that last word: bench/plant pot/bucket/pavement read 'blank' 0.92-1.00, but the 2026-08-17 stone trough read 'bird' 0.49-0.72, and 'bird' is the class-level roll-up in SpeciesNet's taxonomy (432 of its 2498 labels are roll-ups above species rank) — counting one as an identification promoted a trough past the movement gate 12 times in 10 minutes. A label names a species only if the label file fills its genus AND species columns. promote_gap_secs rate-limits it: this is the only place the classifier runs outside an event. verify_person's veto is also one-sided in a second way now — it can demote a person to 'animal', but when SpeciesNet reads the crop as 'blank' the box is DROPPED rather than relabelled, because 'not a person' is not 'therefore an animal' and re-tagging an empty crop is what announced an ANIMAL over an empty trough at 07:45:19.",
  "_comment_gallery": "Crop harvester (see gallery.py): saves face + animal crops (gitignored — biometrics incl. children) to learn from. Faces get an SFace embedding for the cluster-and-label step; near-duplicates and floods are dropped so the set stays diverse. This is how the household (incl. the kids) gets learned and how the species classifier's training set grows.",
  "_comment_talk": "Two-way talk (see talk.py). On a confirmed detection of greet_on ('person'), the camera speaker says greet_text in greet_voice (a macOS `say` voice; Anna is German female). greet_once=true = just the first time per run (a proof); set false + greet_cooldown_secs to greet every visit. Needs H32_CAMERA_DEVID in local.env (capture it with capture/capture_talk.py) — without it, talk is off. Fires only on the post-scenery-filter person, so the bench/rock never trigger it.",
  "_comment_faces": "Face identification (see faces.py). Needs detector/get-face-models.sh and somebody enrolled via detector/enroll.py. Faces are only looked for INSIDE a person box - searching the whole frame finds the plastic bucket. Identity is decided by voting across a visit because a face is only visible in ~38% of frames. threshold 0.40 sits above SFace's own 0.363 and margin 0.10 forces an ambiguous face to 'unknown', because the false-accept rate on this camera is UNMEASURED - see TODO.md. known_suppresses_event stays false until it is measured: turning it on lets a stranger who matches you cancel your own alert."
}
