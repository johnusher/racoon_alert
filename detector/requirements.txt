# h32 detector deps. Install into the project venv:
#   ../.venv/bin/pip install -r requirements.txt
# Then fetch the model:  ./get-model.sh
#
# NOTE: we load MegaDetector v6 weights *directly* through ultralytics (see detect.py) —
# the PytorchWildlife package's own loader pulls an old yolov5 that breaks on Python 3.14,
# so it is deliberately NOT required here.
ultralytics
opencv-python
numpy
torch
torchvision
# for the PTZ capture tooling (capture/):
scapy
open_clip_torch   # legacy CLIP species matcher (species.py) — superseded by speciesnet.py
# SpeciesNet (speciesnet.py) — names the animal and vetoes MegaDetector's bad `person`
# calls. We load the released weights directly (detector/get-speciesnet.sh): the
# `speciesnet` PyPI package needs Python <3.14 and pulls the same old yolov5 noted above.
# Its weights are a torch.fx GraphModule converted from ONNX, so onnx2torch has to be
# importable for torch.load to resolve the pickle — we never call it ourselves.
onnx2torch
